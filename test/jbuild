(jbuild_version 1)

(executable
 ((name        test)
  (libraries (core lang_compiler batteries))
))

;; makes sure the executable is built and run in test mode
(alias
  ((name    runtest)
   (deps ((file test.exe)))
))
