opam-version: "1.2"
maintainer: "mateusz.lewko@gmail.com"

build: [
  ["jbuilder" "build" "--only" "high_ollvm" "--root" "." "-j" jobs "@install"]
]

depends: [
  "jbuilder" {build & >= "1.0+beta14"}
]
